/**
 * @deprecated - Import from '@/app/services/userService' instead
 * This file is a compatibility layer that re-exports from the main implementation.
 */

import { fetchUserProfile } from '@/app/services/userService';

export { fetchUserProfile };

export default {
  fetchUserProfile
}; 