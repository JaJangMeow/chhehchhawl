export type Database = {
  public: {
    Tables: {
      // Add your Supabase tables types here
    }
  }
}

// Adding a default export to avoid Next.js routing warnings
const SupabaseTypes = {}; 
export default SupabaseTypes;
